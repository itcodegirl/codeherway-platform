// ═══════════════════════════════════════════════
// JAVASCRIPT COURSE — 14 Modules, 24 Lessons
// ═══════════════════════════════════════════════

import type { Module } from '../../utils/types';

export const JS_MODULES: Module[] = [
  { id:201, emoji:'⚡', title:'JavaScript Foundations', tagline:'The language that makes the web alive.', lessons:[
    { id:'j1-1', title:'What is JavaScript?',
      content:`**JavaScript** is the programming language of the web. HTML builds structure, CSS adds style, JavaScript adds **behavior** — interactivity, logic, real-time changes.\n\nClick a button and a menu opens? JavaScript. Submit a form? JavaScript. It runs in every browser natively, making it the most important language for web developers.\n\nUnlike HTML and CSS which are declarative (describe what you want), JavaScript is **imperative** — step-by-step instructions.`,
      code:`// This is JavaScript!\nconsole.log("Hello, World!");\n\nlet name = "CodeHerWay";\nconsole.log("Welcome to " + name);\n\nlet hour = new Date().getHours();\nif (hour < 12) {\n    console.log("Good morning!");\n} else {\n    console.log("Good afternoon!");\n}`,
      tip:`Open DevTools (F12) → Console tab. Type JS directly there to experiment.` },
    { id:'j1-2', title:'Adding JS to Your Page',
      content:`**1. External file (BEST)** — separate .js with \`<script defer>\`\n**2. Internal** — \`<script>\` tags in HTML\n**3. Inline handlers** — \`onclick="..."\` AVOID\n\nAlways use \`defer\` — ensures HTML loads before JS runs.`,
      code:`<!-- Best: external with defer -->\n<script src="app.js" defer></script>\n\n<!-- OK: internal -->\n<script>\n    console.log("Runs immediately!");\n</script>\n\n<!-- BAD: inline -->\n<button onclick="alert('messy!')">Don't</button>`,
      challenge:`Create HTML + app.js files. Link with defer. Make a button log to console.`,
      tip:`defer waits for HTML then runs in order. async loads in parallel — good for analytics.` },
    { id:'j1-3', title:'The Console & Debugging',
      content:`The browser **console** is your best friend.\n\n**Methods:** \`console.log()\`, \`console.error()\`, \`console.warn()\`, \`console.table()\`, \`console.time()\`/\`timeEnd()\`\n\n**Debugging:** Read error messages — they include file, line number, and what went wrong. Use \`debugger;\` to pause execution.`,
      code:`console.log("Hello!");\nconsole.log("Name:", "Jenna", "Age:", 25);\nconsole.error("Something broke!");\nconsole.warn("Deprecated feature");\n\nconst user = { name: "Jenna", role: "Dev" };\nconsole.table(user);\n\nconsole.group("User Data");\nconsole.log("Name: Jenna");\nconsole.log("Role: Developer");\nconsole.groupEnd();\n\nconsole.time("loop");\nfor (let i = 0; i < 1000000; i++) {}\nconsole.timeEnd("loop");`,
      tip:`console.table() is underrated. Use it with arrays of objects for clean output.` },
  ]},
  { id:202, emoji:'📦', title:'Variables & Data Types', tagline:'Store data. Name it. Use it.', lessons:[
    { id:'j2-1', title:'let, const, and var',
      content:`**\`const\`** — can't reassign. Use by DEFAULT.\n**\`let\`** — can reassign. Use when value changes.\n**\`var\`** — old way. DON'T use — has scoping issues.\n\n**Naming:** camelCase, start with letter/$/_, case-sensitive.`,
      code:`// const: can't reassign (default!)\nconst appName = "CodeHerWay";\nconst maxUsers = 100;\n\n// let: can reassign\nlet score = 0;\nscore = 10;\nscore += 5; // 15\n\n// const with objects: reference fixed, contents change\nconst colors = ["red", "blue"];\ncolors.push("green"); // OK!\n// colors = [];        // ERROR!\n\n// Naming\nconst firstName = "Jenna";   // camelCase\nconst MAX_RETRIES = 3;       // UPPER_CASE constants`,
      tip:`Use const for everything. Only switch to let when you need to reassign.` },
    { id:'j2-2', title:'Primitive Data Types',
      content:`**7 primitives:** string, number, boolean, undefined, null, symbol, bigint\n\n**string** — \`"hello"\`, \`'hello'\`, template literals\n**number** — integers and decimals\n**boolean** — true or false\n**undefined** — declared but no value\n**null** — intentionally empty\n\nUse \`typeof\` to check type.`,
      code:`const name = "Jenna";\nconst msg = \`Welcome, \${name}!\`; // template literal\nconst age = 25;\nconst price = 29.99;\nconst isLoggedIn = true;\nlet email;            // undefined\nconst deleted = null; // intentionally empty\n\nconsole.log(typeof name);      // "string"\nconsole.log(typeof age);       // "number"\nconsole.log(typeof isLoggedIn); // "boolean"\nconsole.log(typeof null);       // "object" (JS bug!)\n\n// Conversion\nconst str = String(42);       // "42"\nconst num = Number("42");     // 42\nconst bool = Boolean(1);      // true`,
      tip:`Template literals (backticks) are a game-changer for strings with variables.` },
    { id:'j2-3', title:'Strings Deep Dive',
      content:`Strings are **immutable** — methods return NEW strings.\n\n**Key methods:** \`.length\`, \`.includes()\`, \`.startsWith()\`, \`.toUpperCase()\`, \`.slice()\`, \`.replace()\`, \`.split()\`, \`.trim()\`, \`.padStart()\``,
      code:`const name = "Jenna";\nconsole.log(name.length);        // 5\nconsole.log(name[0]);            // "J"\nconsole.log(name.at(-1));        // "a"\nconsole.log(name.includes("enn")); // true\nconsole.log(name.toUpperCase()); // "JENNA"\nconsole.log("  hi  ".trim());    // "hi"\nconsole.log(name.slice(0, 3));   // "Jen"\nconsole.log("Hello World".replace("World", "JS"));\n\nconst csv = "HTML,CSS,JS";\nconsole.log(csv.split(","));     // ["HTML","CSS","JS"]\nconsole.log("42".padStart(5, "0")); // "00042"`,
      challenge:`Write a function: "jenna doe" → "Jenna D." using string methods.` },
  ]},
  { id:203, emoji:'🔀', title:'Operators & Control Flow', tagline:'Make decisions. Control the flow.', lessons:[
    { id:'j3-1', title:'Operators',
      content:`**Arithmetic:** \`+\`, \`-\`, \`*\`, \`/\`, \`%\` (remainder), \`**\` (exponent)\n**Assignment:** \`=\`, \`+=\`, \`-=\`, \`++\`, \`--\`\n**Comparison:** \`===\` (strict), \`!==\`, \`>\`, \`<\`, \`>=\`, \`<=\`\n**Logical:** \`&&\` (AND), \`||\` (OR), \`!\` (NOT)\n\n**CRITICAL:** Always use \`===\` not \`==\`. Double equals coerces types.`,
      code:`console.log(10 % 3);  // 1 (remainder)\nconsole.log(2 ** 3);  // 8 (exponent)\n\nlet score = 10;\nscore += 5;  // 15\nscore++;     // 16\n\n// ALWAYS use ===\nconsole.log(5 === 5);    // true\nconsole.log(5 === "5");  // false (different types!)\nconsole.log(5 == "5");   // true (BAD! coercion)\n\nconst age = 25, hasID = true;\nconst canEnter = age >= 21 && hasID; // true`,
      tip:`=== checks type AND value. == coerces types. Always use ===.` },
    { id:'j3-2', title:'Conditionals',
      content:`**if/else** — standard decisions\n**switch** — one variable, many values\n**ternary** — one-line if/else: \`condition ? A : B\`\n**??** — nullish coalescing (fallback for null/undefined)\n**?.** — optional chaining (safe nested access)`,
      code:`const score = 85;\nif (score >= 90) console.log("A");\nelse if (score >= 80) console.log("B");\nelse console.log("Keep studying!");\n\n// Ternary\nconst status = score >= 70 ? "Pass" : "Fail";\n\n// Switch\nconst day = "Monday";\nswitch (day) {\n    case "Saturday": case "Sunday":\n        console.log("Weekend!"); break;\n    default: console.log("Weekday");\n}\n\n// Nullish coalescing\nconst display = null ?? "Anonymous"; // "Anonymous"\n\n// Optional chaining\nconst user = { profile: { name: "Jenna" } };\nconsole.log(user.settings?.theme); // undefined (no crash!)`,
      challenge:`Grade calculator: 0-100 → letter grade with if/else. Then pass/fail with ternary.` },
    { id:'j3-3', title:'Loops',
      content:`**for** — known iterations\n**while** — unknown iterations\n**for...of** — arrays/strings (PREFERRED)\n**for...in** — object keys\n\n\`break\` exits. \`continue\` skips to next.`,
      code:`for (let i = 0; i < 5; i++) {\n    console.log(i); // 0,1,2,3,4\n}\n\n// for...of (best for arrays!)\nconst skills = ["HTML", "CSS", "JS"];\nfor (const skill of skills) {\n    console.log(skill);\n}\n\n// for...in (for objects)\nconst user = { name: "Jenna", role: "Dev" };\nfor (const key in user) {\n    console.log(\`\${key}: \${user[key]}\`);\n}\n\n// break and continue\nfor (let i = 0; i < 10; i++) {\n    if (i === 3) continue; // skip 3\n    if (i === 7) break;    // stop at 7\n    console.log(i);\n}`,
      tip:`for...of is cleaner for arrays. Save classic for when you need the index.` },
  ]},
  { id:204, emoji:'🔧', title:'Functions', tagline:'Write it once. Use it everywhere.', lessons:[
    { id:'j4-1', title:'Function Basics',
      content:`Three ways:\n**Declaration** — hoisted\n**Expression** — stored in variable\n**Arrow** — modern shorthand (preferred)\n\nArrow functions: shorter syntax, predictable \`this\`.`,
      code:`// Declaration (hoisted)\nfunction greet(name) {\n    return \`Hello, \${name}!\`;\n}\n\n// Expression\nconst add = function(a, b) { return a + b; };\n\n// Arrow (preferred!)\nconst multiply = (a, b) => a * b;\nconst square = n => n ** 2;\n\nconsole.log(multiply(3, 4)); // 12\nconsole.log(square(5));      // 25\n\n// Default parameters\nconst greetUser = (name = "friend") => \`Hey, \${name}!\`;\nconsole.log(greetUser());       // "Hey, friend!"\nconsole.log(greetUser("Jenna")); // "Hey, Jenna!"`,
      tip:`Arrow functions with implicit return (no curly braces) are the most common modern pattern.` },
    { id:'j4-2', title:'Scope & Closures',
      content:`**Scope** = where variables are accessible.\n- **Global** — everywhere\n- **Function** — inside that function\n- **Block** — inside {} with let/const\n\n**Closures** = a function that "remembers" its outer scope even after the outer function finishes.`,
      code:`// Block scope\nif (true) { const inner = "scoped"; }\n// console.log(inner); // ERROR\n\n// Closures\nconst createCounter = () => {\n    let count = 0; // private!\n    return {\n        increment: () => ++count,\n        decrement: () => --count,\n        getCount: () => count,\n    };\n};\n\nconst counter = createCounter();\nconsole.log(counter.increment()); // 1\nconsole.log(counter.increment()); // 2\nconsole.log(counter.getCount());  // 2\n\n// Factory\nconst makeMultiplier = (x) => (y) => x * y;\nconst triple = makeMultiplier(3);\nconsole.log(triple(5)); // 15`,
      challenge:`Create a createBank closure with private balance, deposit, withdraw, getBalance methods.` },
    { id:'j4-3', title:'Callbacks & Higher-Order Functions',
      content:`A **callback** is a function passed as an argument. A **higher-order function** takes or returns functions.\n\nEverywhere in JS: event handlers, array methods, async code, timers.`,
      code:`// Callback\nconst doWork = (callback) => {\n    console.log("Working...");\n    callback();\n};\ndoWork(() => console.log("Done!"));\n\n// setTimeout\nsetTimeout(() => console.log("3s later"), 3000);\n\n// setInterval\nlet count = 0;\nconst timer = setInterval(() => {\n    count++;\n    console.log(\`Tick \${count}\`);\n    if (count >= 5) clearInterval(timer);\n}, 1000);\n\n// Function returning function\nconst withLogging = (fn) => {\n    return (...args) => {\n        console.log("Args:", args);\n        return fn(...args);\n    };\n};\nconst loggedAdd = withLogging((a, b) => a + b);\nloggedAdd(3, 4);`,
      tip:`Higher-order functions are the backbone of modern JS. Get comfortable reading them.` },
  ]},
  { id:205, emoji:'📋', title:'Arrays', tagline:'Lists of data. Endlessly powerful.', lessons:[
    { id:'j5-1', title:'Array Basics',
      content:`Arrays store ordered lists. **Zero-indexed** — first item is position 0.\n\n**Key methods:** push/pop (end), unshift/shift (start), splice (anywhere), includes/indexOf (search).`,
      code:`const skills = ["HTML", "CSS", "JS"];\nconsole.log(skills[0]);     // "HTML"\nconsole.log(skills.at(-1)); // "JS"\n\nskills.push("React");    // add end\nskills.pop();            // remove end\nskills.unshift("Git");   // add start\n\nconsole.log(skills.includes("CSS")); // true\n\n// Spread: copy & merge\nconst more = [...skills, "Node"];\n\n// Destructuring\nconst [first, second, ...rest] = skills;`,
      tip:`Use .at(-1) for last element instead of array[array.length - 1].` },
    { id:'j5-2', title:'Array Methods (The Big Four)',
      content:`The MOST important methods in JavaScript:\n\n**map()** — transform each → new array\n**filter()** — keep matches → new array\n**reduce()** — combine all → single value\n**forEach()** — do something (no return)\n\nNone modify the original. All take callbacks.`,
      code:`const prices = [29.99, 9.99, 49.99, 19.99];\n\n// map: transform\nconst withTax = prices.map(p => p * 1.08);\nconst labels = prices.map(p => \`$\${p.toFixed(2)}\`);\n\n// filter: keep matches\nconst expensive = prices.filter(p => p > 20);\n\n// reduce: combine\nconst total = prices.reduce((sum, p) => sum + p, 0);\n\n// forEach: side effects\nprices.forEach((p, i) => console.log(\`Item \${i+1}: $\${p}\`));\n\n// Chaining!\nconst result = prices\n    .filter(p => p > 20)\n    .map(p => p * 0.9)\n    .reduce((sum, p) => sum + p, 0);\nconsole.log(result.toFixed(2));`,
      challenge:`Given products [{name, price, category}], chain filter→map→reduce.` },
    { id:'j5-3', title:'More Array Methods',
      content:`**Search:** find(), findIndex(), some(), every()\n**Sort:** sort(), toSorted() (non-mutating)\n**Transform:** flat(), slice(), join()`,
      code:`const users = [\n    { name: "Jenna", age: 25, active: true },\n    { name: "Maya", age: 30, active: false },\n    { name: "Kai", age: 22, active: true },\n];\n\nconst jenna = users.find(u => u.name === "Jenna");\nconst idx = users.findIndex(u => u.age > 28); // 1\nconsole.log(users.some(u => u.age > 25));  // true\nconsole.log(users.every(u => u.active));   // false\n\nconst sorted = users.toSorted((a, b) => a.age - b.age);\nconst nested = [[1,2],[3,4]];\nconsole.log(nested.flat()); // [1,2,3,4]\nconsole.log(["HTML","CSS","JS"].join(", "));`,
      tip:`sort() mutates! Use toSorted() or [...arr].sort() to keep the original safe.` },
  ]},
  { id:206, emoji:'🗂️', title:'Objects', tagline:'Key-value pairs. Backbone of JS.', lessons:[
    { id:'j6-1', title:'Object Basics',
      content:`Objects store **key-value pairs**. Access with dot (\`obj.key\`) or bracket (\`obj["key"]\`) notation.\n\n**Destructuring**, **spread**, and **shorthand** are essential patterns.`,
      code:`const user = { name: "Jenna", age: 25, role: "Dev", skills: ["HTML","CSS"] };\n\nconsole.log(user.name);       // "Jenna"\nconsole.log(user["role"]);    // "Dev"\n\nuser.age = 26;                // modify\nuser.company = "CodeHerWay"; // add\ndelete user.skills;           // remove\n\n// Destructuring\nconst { name, age, ...rest } = user;\n\n// Shorthand\nconst x = 10, y = 20;\nconst point = { x, y }; // { x: 10, y: 20 }\n\n// Spread\nconst updated = { ...user, age: 27, level: "Senior" };`,
      tip:`Destructuring objects is one of the most common patterns in modern JS and React.` },
    { id:'j6-2', title:'Object Methods & Iteration',
      content:`**Object.keys()** — array of keys\n**Object.values()** — array of values\n**Object.entries()** — array of [key, value]\n**Object.assign()** — merge\n**Object.freeze()** — immutable`,
      code:`const product = { name: "CSS Course", price: 49, category: "Education" };\n\nconsole.log(Object.keys(product));   // ["name","price","category"]\nconsole.log(Object.values(product)); // ["CSS Course", 49, "Education"]\n\nfor (const [key, value] of Object.entries(product)) {\n    console.log(\`\${key}: \${value}\`);\n}\n\nconsole.log("price" in product); // true\n\n// Merge\nconst defaults = { theme: "dark", lang: "en" };\nconst prefs = { theme: "light" };\nconst config = { ...defaults, ...prefs }; // theme: "light"`,
      challenge:`Create a function comparing two objects: which keys were added, removed, changed.` },
  ]},
  { id:207, emoji:'🌐', title:'The DOM', tagline:'JavaScript meets HTML.', lessons:[
    { id:'j7-1', title:'Selecting Elements',
      content:`The **DOM** is the browser's representation of HTML as a JS object tree.\n\n**querySelector()** — first match (CSS selector)\n**querySelectorAll()** — all matches\n**getElementById()** — by ID (faster)`,
      code:`const heading = document.querySelector("h1");\nconst btn = document.querySelector("#submit-btn");\nconst cards = document.querySelectorAll(".card");\n\ncards.forEach(card => console.log(card.textContent));\n\n// Navigate DOM tree\nconst parent = heading.parentElement;\nconst next = heading.nextElementSibling;\n\n// Check existence\nconst modal = document.querySelector(".modal");\nif (modal) console.log("Found!");`,
      tip:`querySelector uses CSS selectors — anything you target in CSS, you can select in JS.` },
    { id:'j7-2', title:'Modifying Elements',
      content:`**Content:** \`textContent\`, \`innerHTML\`\n**Attributes:** \`getAttribute()\`, \`setAttribute()\`, \`dataset\`\n**Classes (PREFERRED):** \`classList.add/remove/toggle/contains\``,
      code:`const heading = document.querySelector("h1");\nheading.textContent = "New Title";\n\nconst card = document.querySelector(".card");\ncard.innerHTML = "<p>New <strong>content</strong></p>";\n\n// Data attributes\nconst btn = document.querySelector("[data-id]");\nconsole.log(btn.dataset.id);\nbtn.dataset.active = "true";\n\n// Classes (PREFERRED)\ncard.classList.add("active", "highlighted");\ncard.classList.remove("hidden");\ncard.classList.toggle("dark-mode");\nconsole.log(card.classList.contains("active"));`,
      tip:`classList.toggle() for anything on/off — dark mode, menus, accordions.` },
    { id:'j7-3', title:'Creating & Removing Elements',
      content:`**Create:** \`document.createElement()\`\n**Add:** \`appendChild()\`, \`prepend()\`, \`insertAdjacentHTML()\`\n**Remove:** \`element.remove()\``,
      code:`const card = document.createElement("div");\ncard.classList.add("card");\ncard.innerHTML = \`<h3>New Card</h3><p>Created with JS!</p>\`;\ndocument.querySelector(".container").appendChild(card);\n\n// Insert HTML (fast!)\nconst list = document.querySelector("ul");\nlist.insertAdjacentHTML("beforeend", '<li class="new">New item</li>');\n\n// Remove\nconst old = document.querySelector(".outdated");\nif (old) old.remove();\n\n// Build list from data\nconst skills = ["HTML", "CSS", "JS"];\nconst ul = document.createElement("ul");\nskills.forEach(s => {\n    const li = document.createElement("li");\n    li.textContent = s;\n    ul.appendChild(li);\n});\ndocument.body.appendChild(ul);`,
      challenge:`Build a todo list: input, add button, list. Add creates li with text + delete button.` },
  ]},
  { id:208, emoji:'🎯', title:'Events', tagline:'Make your page respond.', lessons:[
    { id:'j8-1', title:'Event Listeners',
      content:`**addEventListener** is the proper way. Never inline handlers.\n\n**Common:** click, submit, input, change, keydown, keyup, mouseover, scroll, load`,
      code:`const btn = document.querySelector("#btn");\nbtn.addEventListener("click", () => console.log("Clicked!"));\n\n// Event object\nbtn.addEventListener("click", (e) => {\n    console.log(e.type);    // "click"\n    console.log(e.target);  // the element\n});\n\n// Keyboard\ndocument.addEventListener("keydown", (e) => {\n    if (e.key === "Escape") console.log("Escape!");\n});\n\n// Form submit\nconst form = document.querySelector("form");\nform.addEventListener("submit", (e) => {\n    e.preventDefault(); // stop reload!\n    const data = new FormData(form);\n    console.log(data.get("email"));\n});\n\n// Input (every keystroke)\nconst input = document.querySelector("input");\ninput.addEventListener("input", (e) => console.log(e.target.value));`,
      tip:`Always e.preventDefault() on form submit. Without it, the page reloads.` },
    { id:'j8-2', title:'Event Delegation & Bubbling',
      content:`**Bubbling** = events travel up the DOM tree.\n**Delegation** = one listener on parent handles all children. More performant, works for dynamic elements.`,
      code:`// One listener handles all items\nconst list = document.querySelector(".todo-list");\nlist.addEventListener("click", (e) => {\n    if (e.target.matches(".delete-btn")) {\n        e.target.closest("li").remove();\n    }\n    if (e.target.matches(".todo-text")) {\n        e.target.classList.toggle("done");\n    }\n});\n\n// Works for dynamically added items too!\nconst newItem = document.createElement("li");\nnewItem.innerHTML = '<span class="todo-text">New</span><button class="delete-btn">X</button>';\nlist.appendChild(newItem);\n\n// Close modal on backdrop click\nmodal.addEventListener("click", (e) => {\n    if (e.target === modal) modal.classList.remove("open");\n});`,
      tip:`Event delegation: one listener, all children. Use e.target.matches() to check what was clicked.` },
  ]},
  { id:209, emoji:'⏳', title:'Async JavaScript', tagline:'Handle the future.', lessons:[
    { id:'j9-1', title:'Promises & Fetch',
      content:`JavaScript is **asynchronous** — it doesn't wait for slow operations.\n\n**Promises** represent future values. States: pending, fulfilled, rejected.\n**fetch()** makes HTTP requests and returns a promise.`,
      code:`fetch("https://jsonplaceholder.typicode.com/users/1")\n    .then(res => res.json())\n    .then(user => {\n        console.log(user.name);\n        console.log(user.email);\n    })\n    .catch(err => console.error("Failed:", err));\n\n// Create a promise\nconst wait = (ms) => new Promise(resolve => setTimeout(resolve, ms));\nwait(2000).then(() => console.log("2s later!"));\n\n// Promise.all: parallel\nPromise.all([\n    fetch("/api/users").then(r => r.json()),\n    fetch("/api/posts").then(r => r.json()),\n]).then(([users, posts]) => console.log(users, posts));`,
      tip:`Always include .catch(). Unhandled errors silently fail — hardest bugs to find.` },
    { id:'j9-2', title:'Async/Await',
      content:`**async/await** makes async code read like synchronous. Mark function \`async\`, use \`await\` before promises. \`try/catch\` for errors.`,
      code:`const getUser = async (id) => {\n    try {\n        const res = await fetch(\n            \`https://jsonplaceholder.typicode.com/users/\${id}\`\n        );\n        if (!res.ok) throw new Error(\`HTTP \${res.status}\`);\n        const user = await res.json();\n        console.log(user.name);\n        return user;\n    } catch (err) {\n        console.error("Failed:", err.message);\n    }\n};\n\ngetUser(1);\n\n// POST request\nconst createPost = async (data) => {\n    const res = await fetch("/api/posts", {\n        method: "POST",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify(data),\n    });\n    return res.json();\n};`,
      challenge:`Build a user search: input + button. Fetch from jsonplaceholder API, display name/email.` },
  ]},
  { id:210, emoji:'🗄️', title:'Local Storage', tagline:'Save data in the browser.', lessons:[
    { id:'j10-1', title:'Browser Storage',
      content:`**localStorage** persists key-value pairs across sessions. Strings only!\n\n**Methods:** setItem, getItem, removeItem, clear\n\nUse **JSON.stringify()** to save objects and **JSON.parse()** to read them back.`,
      code:`localStorage.setItem("theme", "dark");\nconst theme = localStorage.getItem("theme"); // "dark"\n\n// Objects: must stringify!\nconst user = { name: "Jenna", level: 5 };\nlocalStorage.setItem("user", JSON.stringify(user));\nconst saved = JSON.parse(localStorage.getItem("user"));\nconsole.log(saved.name); // "Jenna"\n\nlocalStorage.removeItem("theme");\n\n// Safe read with fallback\nconst getStored = (key, fallback) => {\n    try {\n        const item = localStorage.getItem(key);\n        return item ? JSON.parse(item) : fallback;\n    } catch { return fallback; }\n};\nconst prefs = getStored("prefs", { theme: "light" });`,
      tip:`Always wrap JSON.parse in try/catch. Corrupted data crashes your app.` },
  ]},
  { id:211, emoji:'🧩', title:'Destructuring & Spread', tagline:'Write less. Do more.', lessons:[
    { id:'j11-1', title:'Modern JS Syntax',
      content:`**Destructuring** — extract from objects/arrays\n**Spread (...)** — expand arrays/objects\n**Rest (...)** — collect remaining items\n**Optional chaining (?.)** — safe access\n**Nullish coalescing (??)** — fallback values`,
      code:`// Object destructuring\nconst { name, age } = { name: "Jenna", age: 25, role: "Dev" };\n\n// Rename + defaults\nconst { name: userName, theme = "dark" } = user;\n\n// Array destructuring\nconst [first, second, ...rest] = [1, 2, 3, 4, 5];\n\n// Swap\nlet a = 1, b = 2;\n[a, b] = [b, a];\n\n// Spread: copy & merge\nconst arr2 = [...arr1, 3, 4];\nconst obj2 = { ...obj1, b: 2 };\n\n// Function param destructuring\nconst greet = ({ name, role = "User" }) => \`Hi \${name}, \${role}\`;\ngreet({ name: "Jenna", role: "Dev" });`,
      tip:`Destructuring in function params is the React way: ({name, age}) => ...` },
  ]},
  { id:212, emoji:'🏗️', title:'Classes & OOP', tagline:'Blueprints for data.', lessons:[
    { id:'j12-1', title:'Classes',
      content:`**Classes** are blueprints for objects. Syntactic sugar over prototypes.\n\n**constructor()** — runs on \`new\`\n**this** — current instance\n**static** — on class, not instances\n**extends** — inheritance`,
      code:`class User {\n    constructor(name, email) {\n        this.name = name;\n        this.email = email;\n        this.isActive = true;\n    }\n    greet() { return \`Hi, I'm \${this.name}\`; }\n    static createGuest() {\n        return new User("Guest", "guest@example.com");\n    }\n}\n\nconst jenna = new User("Jenna", "jenna@code.com");\nconsole.log(jenna.greet());\n\nclass Admin extends User {\n    constructor(name, email, level) {\n        super(name, email);\n        this.level = level;\n    }\n    promote(user) {\n        console.log(\`\${this.name} promoted \${user.name}\`);\n    }\n}`,
      challenge:`Create a TodoList class with addTask, removeTask, toggleComplete, getActive, getCompleted.` },
  ]},
  { id:213, emoji:'🛠️', title:'Error Handling & Modules', tagline:'Robust, organized code.', lessons:[
    { id:'j13-1', title:'Error Handling',
      content:`**try/catch/finally** — catch errors gracefully\n**throw** — create custom errors\n**Error types:** TypeError, ReferenceError, SyntaxError, RangeError`,
      code:`try {\n    const data = JSON.parse("invalid json");\n} catch (error) {\n    console.error("Parse failed:", error.message);\n} finally {\n    console.log("Always runs");\n}\n\nconst validateAge = (age) => {\n    if (typeof age !== "number") throw new TypeError("Must be number");\n    if (age < 0 || age > 150) throw new RangeError("Must be 0-150");\n    return true;\n};\n\ntry { validateAge("twenty"); }\ncatch (e) {\n    if (e instanceof TypeError) console.log("Wrong type:", e.message);\n}`,
      tip:`Never leave catch blocks empty. Silent failures are the hardest bugs.` },
    { id:'j13-2', title:'ES Modules',
      content:`Split code into files with **import/export**.\n\n**Named exports** — multiple per file\n**Default export** — one main per file\n\nRequires \`type="module"\` on script tag or a build tool.`,
      code:`// ─── math.js ───\nexport const add = (a, b) => a + b;\nexport const PI = 3.14159;\n\n// ─── User.js ───\nexport default class User {\n    constructor(name) { this.name = name; }\n}\n\n// ─── app.js ───\nimport { add, PI } from "./math.js";\nimport User from "./User.js";\nimport * as math from "./math.js";\n\nconsole.log(math.add(1, 2));\nconsole.log(math.PI);\n\n// In HTML:\n// <script type="module" src="app.js"></script>`,
      tip:`One file, one responsibility. utils.js, User.js, api.js — professional structure.` },
  ]},
  { id:214, emoji:'🚀', title:'JS Best Practices', tagline:'Write JS your future self will love.', lessons:[
    { id:'j14-1', title:'Modern JS Patterns',
      content:`**Clean code:** const default, arrow functions, destructuring, template literals, optional chaining, early returns`,
      code:`// Early returns\nconst processUser = (user) => {\n    if (!user) return null;\n    if (!user.isActive) return "Inactive";\n    return \`Welcome, \${user.name}!\`;\n};\n\n// Object shorthand\nconst createUser = (name, email) => ({ name, email, createdAt: new Date() });\n\n// Array methods over loops\nconst activeEmails = users.filter(u => u.isActive).map(u => u.email);\n\n// Async pattern\nconst api = {\n    get: async (url) => {\n        try {\n            const res = await fetch(url);\n            if (!res.ok) throw new Error(res.statusText);\n            return { data: await res.json(), error: null };\n        } catch (error) {\n            return { data: null, error: error.message };\n        }\n    },\n};`,
      tip:`You've completed JavaScript! Next: React, Node.js, or whatever excites you.` },
    { id:'j14-2', title:'Your JavaScript Toolkit',
      content:`**Tools:** DevTools, MDN, ESLint, Prettier\n**Frameworks:** React, Next.js, Vue, Svelte\n**Backend:** Node.js, Express, Deno\n**Build:** Vite, npm/pnpm\n**Testing:** Jest, Vitest, Playwright\n\nMaster vanilla JS first. Frameworks change — fundamentals are forever.`,
      code:`// Starter patterns:\ndocument.addEventListener("DOMContentLoaded", () => {\n    // Your code here\n});\n\nconst btn = document.querySelector("#btn");\nbtn?.addEventListener("click", handleClick);\n\nconst getData = async (url) => {\n    try {\n        const res = await fetch(url);\n        if (!res.ok) throw new Error(res.statusText);\n        return await res.json();\n    } catch (err) {\n        console.error(err);\n        return null;\n    }\n};`,
      challenge:`Build a bookmark manager: add, delete, search, persist to localStorage. Ship it.` },
  ]},
];
